import type { Story } from '@ladle/react';
import { Button } from '../button';

export const Default: Story = () => <Button>Click me!</Button>;
export const Primary: Story = () => <Button color="primary">Click me!</Button>;
